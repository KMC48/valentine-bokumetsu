# 依存パッケージとビルド成果物
node_modules/
app/dist/
app/node_modules/

# 納品された生の素材（OneDriveに原本がある。リポジトリには入れない）。
# 画像素材/ は制作指示書のMDだけ残す。
画像素材/*
!画像素材/*.md
サウンド素材/
追加素材0914-b/
# 素材の作業用ファイルとWAVの原本。
# public/ の外に置いてある（public/ に置くとビルド成果物に入ってしまう）。
app/素材作業用/

# Android（署名情報とビルド成果物）
app/android/keystore.properties
app/android/local.properties
app/android/*.jks
app/android/.gradle/
app/android/build/
app/android/app/build/
app/android/capacitor-cordova-android-plugins/

# 加工スクリプトの中間ファイル
scripts/__pycache__/
scripts/_*.txt
scripts/_*.json

# 確認用スクショ（39MB。手元で見るためのもの）
確認用スクショ/

# OS
Thumbs.db
desktop.ini
.DS_Store
