# バレンタイン撲滅委員会  
## AIコーディングエージェント向け実装指示書 / MVP基盤仕様

> 目的：本ドキュメントをもとに、AIコーディングエージェントが「バレンタイン撲滅委員会」のゲーム基盤を実装できる状態にする。  
> 最初から全画像・全シナリオを完成させる必要はない。まずは **ゲーム進行・スコア・通報・ステージ遷移・周回・エンディング分岐が動くMVP** を作る。  
> 画面素材は順次追加するため、現時点ではプレースホルダー画像で代替できる構造にする。

---

# 1. ゲーム概要

## タイトル
**バレンタイン撲滅委員会**

## ジャンル
学園探索 / 観察 / 通報アクション / ブラックコメディ / 周回型ADV

## 対応想定
まずは **スマートフォン縦画面向けWebゲーム** として実装する。

- 基準サイズ：1080 × 1920 px
- アスペクト比：9:16
- PCブラウザでも中央にスマートフォン比率で表示
- タッチ操作優先
- マウス操作にも対応

---

# 2. 世界観

舞台は共学の高校。

この学校では校則により、学校へのお菓子の持ち込みは禁止されている。

しかし2月14日のバレンタインデーになると、女子生徒たちは校則を破り、友チョコや本命チョコを学校へ持ち込む。

主人公はモテない男子高校生。

本人は恋愛への嫉妬ではなく、

> 「校則違反を取り締まっているだけだ」

と主張し、自らを **「バレンタイン撲滅委員会」** と名乗って校内を巡回する。

チョコを持ち込んだ生徒を発見し、生徒指導の先生へ通報することでポイントを獲得する。

---

# 3. ゲームのコア体験

このゲームの面白さは、単なる「チョコ探し」ではなく、

**怪しい女子生徒を観察し、チョコ所持の可能性と、友チョコか本命チョコかを見極め、正しく通報すること**

にある。

ゲームの基本ループは以下。

```text
探索
↓
怪しい女子生徒を発見
↓
観察
↓
ヒントを集める
↓
友チョコ / 本命チョコ / 無実 を推理
↓
通報
↓
結果判定
↓
ポイント獲得 or ペナルティ
↓
次のターゲットへ
```

---

# 4. ゲーム全体の進行

1周のゲームフローは固定で以下。

```text
タイトル
↓
オープニングストーリー
↓
朝の登校ステージ
↓
中間ストーリー①
↓
昼休みステージ
↓
中間ストーリー②
↓
放課後ステージ
↓
結果画面
↓
エンディング分岐
↓
周回クリア
↓
次の学校が解放
```

---

# 5. 1周目〜3周目の構成

## 1周目：普通の公立高校

テーマ：

**「探す」**

特徴：

- チョコを比較的見つけやすい
- ラッピングが見えている
- 紙袋を持っている
- 女子生徒の挙動が分かりやすい
- チュートリアルを兼ねる
- 誤認しにくい

難易度：

**NORMAL**

---

## 2周目：私立進学校

テーマ：

**「見破る」**

特徴：

- 女子生徒がチョコを上手く隠している
- 紙袋ではなく教科書ケースや弁当箱に隠す
- 友達同士でチョコを預け合う
- ダミーの紙袋を持つ
- 会話や行動から推理する必要がある
- 本命チョコの判別が難しくなる

難易度：

**HARD**

---

## 3周目：超難関エリート校

テーマ：

**「推理する」**

特徴：

- チョコを直接見せない
- 会話、移動、時間、人間関係から推理
- ロッカーへの事前配置
- ダミー情報
- 偽の呼び出し
- 女子同士の連携
- 男子生徒が見張り役になる
- 本命チョコは心理戦レベル

難易度：

**VERY HARD**

---

# 6. ステージ構成

各周回は以下の3ステージ。

## 朝：登校ステージ

主な場所：

- 校門
- 昇降口
- 廊下
- 下駄箱

特徴：

- チュートリアル寄り
- チョコが見つけやすい
- 友チョコが多い
- 本命チョコは少なめ

---

## 昼休みステージ

主な場所：

- 教室
- 廊下
- 階段
- 購買
- 中庭

特徴：

- 難易度上昇
- 友チョコと本命チョコが混在
- 複数人が同時に動く
- 会話ヒントが増える
- フェイクも登場

---

## 放課後ステージ

主な場所：

- 体育館裏
- 屋上前
- 部室棟
- 中庭
- 校門

特徴：

- 最終ステージ
- 本命チョコ率が高い
- 高得点
- 制限時間が厳しい
- 「告白直前イベント」が発生
- 周回ごとのボス的女子生徒を配置可能

---

# 7. スコアシステム

スコア値は設定ファイルから簡単に変更できるようにする。

初期値：

```ts
export const SCORE = {
  FRIEND_CHOCOLATE: 100,
  HONMEI_CHOCOLATE: 500,
  HONMEI_BEFORE_CONFESSION: 1000,
  FALSE_REPORT: -300,
};
```

## 友チョコ

通報成功：

**+100pt**

特徴：

- 比較的多く出現
- 見つけやすい
- 基本得点源

---

## 本命チョコ

通報成功：

**+500pt**

特徴：

- 出現頻度は低い
- 見極めが難しい
- 高得点
- 放課後に多い

---

## 告白直前の本命チョコ

通報成功：

**+1000pt**

条件例：

- 体育館裏
- 屋上前
- 放課後
- 男子生徒が近くにいる
- 呼び出し済み

ボーナスターゲット扱い。

---

## 誤認通報

チョコを持っていない生徒を通報：

**-300pt**

さらに：

- コンボリセット
- 信用度低下
- 演出として主人公が怒られる

---

# 8. コンボ

連続で正しい通報をするとコンボが増える。

```text
1〜2連続：通常
3連続：×1.2
5連続：×1.5
10連続：×2.0
```

誤認通報で0に戻る。

コンボ倍率は設定ファイル化する。

---

# 9. ターゲットデータ構造

女子生徒はデータ駆動で追加できるようにする。

例：

```ts
type ChocolateType =
  | "none"
  | "friend"
  | "honmei"
  | "honmei_special";

type Student = {
  id: string;
  name: string;

  image: string;

  chocolateType: ChocolateType;

  stage: "morning" | "lunch" | "afterSchool";

  position: {
    x: number;
    y: number;
  };

  suspicionLevel: number;

  hints: string[];

  dialogue?: string[];

  behavior?: string[];

  isDecoy?: boolean;

  targetBoyId?: string;

  reportResultText?: string;
};
```

---

# 10. 観察システム

怪しい女子生徒をタップすると観察モードに入る。

観察モードでは以下を表示。

- 名前
- 表情
- 所持品
- 怪しい仕草
- 会話
- 周囲の人物
- チョコらしき物の有無
- 推理用ヒント

例：

```text
「何度も鞄の中を確認している」
「放課後、体育館裏に来てと言っていた」
「同じ袋を5個持っている」
```

これらの情報から、

- 友チョコ
- 本命チョコ
- 無実

をプレイヤーが判断する。

---

# 11. 通報システム

観察中、またはターゲット選択中に

**「通報」**

ボタンを押す。

通報時：

```text
通報
↓
生徒指導の先生が登場
↓
所持品確認
↓
正解判定
↓
ポイント加算
↓
結果演出
```

---

# 12. 通報結果演出

## 友チョコ

```text
友チョコを発見！

+100pt
```

---

## 本命チョコ

```text
本命チョコを摘発！

+500pt
```

通常より派手な演出。

---

## 告白直前

```text
告白直前を阻止！

+1000pt
```

特殊演出。

---

## 誤認

```text
チョコは見つからなかった……

誤認通報！

-300pt
```

---

# 13. 制限時間

各ステージにタイマーを設定。

例：

```text
朝：180秒
昼：180秒
放課後：240秒
```

タイマー0で自動終了。

開発時にはデバッグ設定で無制限にもできるようにする。

---

# 14. ミニマップ

右上にミニマップを表示。

表示内容：

- 主人公位置
- 怪しい生徒
- 教師
- イベント地点

ただし、

**怪しい生徒＝チョコ所持確定**

ではない。

難易度が上がるほどミニマップ情報を減らす。

---

# 15. ストーリーパート

Visual Novel形式。

UI：

```text
背景画像
キャラクター
名前
セリフ
NEXT
```

操作：

- タップで次へ
- スキップ
- オート
- ログ

最低限MVPでは

**タップで次へ**

だけ実装すればよい。

シナリオデータはJSONまたはTSで管理。

例：

```ts
type StoryLine = {
  speaker: string;
  text: string;
  character?: string;
  background?: string;
};
```

---

# 16. 結果画面

3ステージ終了後に表示。

表示項目：

- TOTAL SCORE
- 通報成功数
- 友チョコ摘発数
- 本命チョコ摘発数
- 告白直前阻止数
- 誤認通報数
- 最大コンボ
- 撲滅率
- ランク

---

# 17. ランク

例：

```text
S
A
B
C
D
```

初期基準：

```text
S：5000pt以上
A：3500pt以上
B：2500pt以上
C：1500pt以上
D：1500pt未満
```

設定ファイルで変更可能にする。

---

# 18. エンディング分岐

結果に応じてエンディングを分岐。

例：

## Sランク

### 「バレンタイン完全撲滅」

校内からチョコが消える。

主人公：

> 「これで……この学校に平和が戻った。」

---

## Aランク

### 「生徒指導の右腕」

先生から謎の信頼を得る。

---

## Bランク

### 「そこそこ撲滅」

多少のチョコは残る。

---

## C以下

### 「バレンタイン防衛成功」

女子生徒たちに完全に出し抜かれる。

---

## 特殊エンディング

条件例：

- 本命チョコを一定数摘発
- 誤認が少ない
- 特定イベント発生

最後に主人公自身が女子生徒からチョコを渡される。

```text
主人公：
「これは……校則違反だ」

女子：
「じゃあ返して」

主人公：
「いや……証拠品として預かる」
```

---

# 19. 周回システム

ゲームクリア時：

```text
1周目クリア
↓
2周目解放
↓
2周目クリア
↓
3周目解放
```

保存データ：

```ts
type SaveData = {
  unlockedLoop: 1 | 2 | 3;
  bestScores: {
    loop1?: number;
    loop2?: number;
    loop3?: number;
  };

  endings: string[];

  settings: {
    bgmVolume: number;
    seVolume: number;
  };
};
```

ブラウザでは

**localStorage**

を使用。

---

# 20. 周回ごとの難易度差

```ts
const LOOP_CONFIG = {
  1: {
    suspicionVisibility: 1.0,
    fakeRate: 0.1,
    hintCount: 3,
  },

  2: {
    suspicionVisibility: 0.7,
    fakeRate: 0.3,
    hintCount: 2,
  },

  3: {
    suspicionVisibility: 0.4,
    fakeRate: 0.5,
    hintCount: 1,
  },
};
```

数値は調整可能にする。

---

# 21. 画面一覧

最低限以下の画面を作る。

```text
TitleScreen
StoryScreen
StageScreen
ObservationModal
ReportResultModal
StageResultScreen
FinalResultScreen
EndingScreen
LoopSelectScreen
SettingsScreen
```

---

# 22. UIレイアウト

ステージ画面は添付の参考画像をベースにする。

参考画像：

**`valentine_annihilation_gameplay_reference.png`**

主なUI：

```text
┌─────────────────────────────┐
│ タイトル             MENU    │
├──────────────┬──────────────┤
│ SCORE        │ TIMER        │
│ COMBO        │              │
├─────────────────────────────┤
│ MISSION                     │
├─────────────────────────────┤
│                             │
│                             │
│        GAME AREA            │
│                             │
│                             │
│                      MAP    │
│                             │
│                [ 通報 ]      │
├─────────────────────────────┤
│ 主人公セリフ / UI            │
├─────────────────────────────┤
│ HOME / STORY / 図鑑 / etc   │
└─────────────────────────────┘
```

---

# 23. 既存参考画像について

現在用意されている画像：

```text
valentine_annihilation_gameplay_reference.png
```

これは

**完成UIをそのまま1枚絵としてゲームに使用する素材ではなく、画面設計・トーン・レイアウトの参考画像**

として扱う。

実装では、

- 背景
- キャラクター
- UI
- ターゲットマーカー
- ボタン
- テキスト

を可能な限り別レイヤーで構築する。

---

# 24. 画像素材の扱い

現時点では素材不足のため、

```text
/public/assets/
```

以下にプレースホルダーを配置する。

推奨構成：

```text
public/
└ assets/
   ├ backgrounds/
   │  ├ school_morning.webp
   │  ├ school_lunch.webp
   │  └ school_after.webp
   │
   ├ characters/
   │  ├ player.webp
   │  ├ teacher.webp
   │  ├ girl_001.webp
   │  ├ girl_002.webp
   │  └ ...
   │
   ├ ui/
   │  ├ report_button.svg
   │  ├ target_marker.svg
   │  └ ...
   │
   └ reference/
      └ gameplay_reference.png
```

素材が存在しない場合は

- 単色
- 仮画像
- SVG
- CSS描画

で代替する。

**素材不足を理由に実装を止めないこと。**

---

# 25. 今後追加予定の画像素材

後から以下を差し替える。

- タイトル背景
- 主人公立ち絵
- 生徒指導教師
- 女子生徒
- 男子生徒
- 各学校背景
- 朝背景
- 昼背景
- 放課後背景
- 体育館裏
- 屋上
- 校門
- 教室
- 通報演出
- エンディングCG
- 周回2学校
- 周回3学校

画像パスをデータ側で指定し、

**コードを変更せず素材だけ差し替えられる構造**

にする。

---

# 26. 推奨技術構成

MVPは以下を推奨。

```text
React
TypeScript
Vite
Zustand
CSS Modules または Tailwind CSS
```

ゲームエンジンは初期MVPでは使用しない。

理由：

- 画面遷移中心
- 2D固定画面
- タップ判定中心
- ADV要素が大きい
- AIエージェントが保守しやすい

将来、自由移動や複雑なアニメーションを追加する場合は Phaser 等への移行を検討する。

---

# 27. 状態管理

Zustand推奨。

例：

```ts
type GameState = {
  loop: number;
  stage: "story" | "morning" | "lunch" | "afterSchool" | "result";

  score: number;
  combo: number;

  correctReports: number;
  falseReports: number;

  friendChocolateCount: number;
  honmeiChocolateCount: number;

  timeRemaining: number;

  selectedStudentId?: string;

  addScore: (score: number) => void;
  reportStudent: (id: string) => void;
  nextStage: () => void;
};
```

---

# 28. データ駆動設計

可能な限りゲーム内容をコードにベタ書きしない。

以下はデータファイル化する。

```text
students.ts
stages.ts
stories.ts
endings.ts
loops.ts
score.ts
```

将来的にJSON化してもよい。

---

# 29. ディレクトリ構成

推奨：

```text
src/
├ app/
│  └ App.tsx
│
├ components/
│  ├ HUD/
│  ├ MiniMap/
│  ├ ReportButton/
│  ├ DialogueBox/
│  └ Modal/
│
├ screens/
│  ├ TitleScreen.tsx
│  ├ StoryScreen.tsx
│  ├ StageScreen.tsx
│  ├ ResultScreen.tsx
│  ├ EndingScreen.tsx
│  └ LoopSelectScreen.tsx
│
├ game/
│  ├ score.ts
│  ├ report.ts
│  ├ ending.ts
│  └ difficulty.ts
│
├ data/
│  ├ students.ts
│  ├ stories.ts
│  ├ stages.ts
│  ├ loops.ts
│  └ endings.ts
│
├ store/
│  └ gameStore.ts
│
├ types/
│  └ game.ts
│
└ styles/
```

---

# 30. MVPで必ず実装するもの

優先度A。

1. タイトル画面
2. ストーリー画面
3. 朝ステージ
4. 昼ステージ
5. 放課後ステージ
6. 女子生徒タップ
7. 観察モーダル
8. 通報
9. 友チョコ判定
10. 本命チョコ判定
11. 誤認判定
12. スコア
13. コンボ
14. タイマー
15. ステージ遷移
16. 最終結果
17. エンディング分岐
18. 1〜3周目解放
19. localStorage保存
20. スマホ縦画面対応

---

# 31. MVPでは後回しにするもの

優先度B/C。

- 複雑なキャラクターアニメーション
- ボイス
- BGM
- 高度なミニマップ
- 自由移動
- ガチャ
- オンラインランキング
- アカウント機能
- 課金
- SNS共有
- 実績システム
- 大量のキャラクター

---

# 32. 仮キャラクター数

MVPでは各ステージ5人程度。

```text
朝：5人
昼：5人
放課後：5人
```

合計15ターゲット。

内訳例：

```text
無実：5
友チョコ：6
本命：3
特殊本命：1
```

データ追加だけで人数を増やせるようにする。

---

# 33. ターゲット配置

背景上にabsolute配置。

例：

```tsx
<StudentTarget
  student={student}
  style={{
    left: `${student.position.x}%`,
    top: `${student.position.y}%`,
  }}
/>
```

画像サイズが変わっても位置が大きく崩れないよう、

**pxではなく%ベース**

を推奨。

---

# 34. スマートフォンUI

最大幅：

```css
.game-root {
  width: 100%;
  max-width: 540px;
  aspect-ratio: 9 / 16;
}
```

PCでは中央配置。

スマホでは全幅。

---

# 35. デバッグモード

開発効率向上のため、

```text
?debug=true
```

などでデバッグモードを有効化。

デバッグ表示：

- 生徒ID
- chocolateType
- 得点
- 当たり判定
- タイマー停止
- ステージ強制移動
- 周回強制変更
- スコア追加

---

# 36. 最初の実装ゴール

最初のコミットでは、

**画像がすべて仮でもよいので、以下が通しで動く状態**

を完成させる。

```text
タイトル
↓
ストーリー
↓
朝
↓
ストーリー
↓
昼
↓
ストーリー
↓
放課後
↓
結果
↓
エンディング
```

さらに、

```text
女子生徒をタップ
↓
観察
↓
通報
↓
判定
↓
ポイント
```

まで実装する。

---

# 37. AIエージェントへの重要指示

以下を必ず守ること。

1. 最初から完成版を作ろうとしない。
2. ゲームロジックを先に作る。
3. 画像不足で作業を止めない。
4. 仮素材で画面を完成させる。
5. キャラクター・ステージ・ストーリーはデータ駆動にする。
6. 後から画像を簡単に差し替えられるようにする。
7. スコア値をコード中に散らさない。
8. 周回難易度を設定ファイルで変更できるようにする。
9. スマホ9:16を最優先する。
10. 既存参考画像のUIトーンを参考にする。
11. 参考画像を単純に背景1枚として実装しない。
12. コンポーネントを必要以上に巨大化させない。
13. TypeScriptの型を定義する。
14. 主要ロジックには最低限のテストを作る。
15. READMEに起動方法を書く。

---

# 38. 実装順序

推奨順序：

```text
STEP 1
プロジェクト作成
↓
STEP 2
型定義
↓
STEP 3
ゲーム状態管理
↓
STEP 4
画面遷移
↓
STEP 5
ストーリー
↓
STEP 6
ステージ
↓
STEP 7
ターゲット
↓
STEP 8
観察
↓
STEP 9
通報
↓
STEP 10
スコア
↓
STEP 11
結果
↓
STEP 12
エンディング
↓
STEP 13
周回
↓
STEP 14
保存
↓
STEP 15
UI調整
```

---

# 39. テスト

最低限以下をテスト。

```text
友チョコ通報 → +100
本命通報 → +500
特殊本命 → +1000
誤認通報 → -300
誤認 → コンボ0
3連続成功 → コンボ倍率
ステージ終了 → 次ステージ
最終ステージ終了 → Result
条件 → Ending分岐
1周目クリア → 2周目解放
2周目クリア → 3周目解放
localStorage保存
```

---

# 40. 完成条件

MVP完成条件：

- npm install が成功
- npm run dev で起動
- TypeScriptエラーなし
- スマートフォン9:16表示
- 1プレイを最後まで完走可能
- スコア計算が正常
- 本命と友チョコで得点が異なる
- 誤認通報が存在
- エンディングが分岐
- 周回が解放される
- ブラウザ更新後も進行保存
- 画像を差し替え可能

---

# 41. 将来的な拡張候補

MVP完成後に検討。

- 生徒図鑑
- アイテム
- スキル
- 本命推理システム
- 人間関係図
- イベントCG
- キャラクターボイス
- BGM / SE
- ランキング
- 実績
- 隠しエンディング
- 4周目以降
- バレンタイン以外のイベント
- ホワイトデー編
- クリスマス編
- 文化祭編

---

# 42. 最終的に目指すゲーム性

周回ごとにゲーム体験を変える。

```text
1周目
探すゲーム

↓

2周目
見破るゲーム

↓

3周目
推理するゲーム
```

最終的には、

**「女子生徒の行動を観察して、本命チョコを見抜き、高得点で通報する」**

ことが最も重要なゲーム体験になる。

---

# 43. コーディングエージェントへの開始指示

この仕様書を読んだら、まず以下を実施すること。

```text
1. React + TypeScript + Viteでプロジェクトを作成
2. 推奨ディレクトリを作る
3. ゲーム状態の型を定義
4. ZustandでGameStoreを実装
5. Title → Story → Stage → Result → Ending の画面遷移を作る
6. 仮画像でMorningステージを作る
7. 3人の女子生徒データを作る
8. friend / honmei / none を1人ずつ設定
9. タップ → 観察 → 通報 → 得点処理を実装
10. ここまで動いた時点で一度動作確認する
```

いきなり全機能を作らず、

**最小のゲームループを成立させてから拡張すること。**

---

以上。
